import React from "react";
import Button from "./Button";

const LocationButton = () => {
  return (
    <div className="gap-2 ">
      <Button></Button>
      <Button></Button>
    </div>
  );
};

export default LocationButton;
